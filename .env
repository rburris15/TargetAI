# Embedding model — biomedical Sentence-Transformers checkpoint.
EMBEDDING_MODEL=pritamdeka/S-Biomed-Roberta-snli-multinli-stsb

# Cross-encoder reranker
RERANKER_MODEL=BAAI/bge-reranker-base

# FAISS index path
INDEX_DIR=./data/index

# Data dirs
RAW_DIR=./data/raw
CLEAN_DIR=./data/clean

# LLM (Ollama)
OLLAMA_MODEL=biomistral
OLLAMA_HOST=http://localhost:11434

# Fetch sizes (keep small for MVP)
CTG_MAX_STUDIES=200
PUBMED_MAX_PMIDS=200
OPENTARGETS_MAX=200